# Security Audit — 2026-07-08

**Tool:** `npm audit --audit-level=high`

## HIGH severity vulnerabilities

| Package | Via | Severity | Advisory |
|---------|-----|----------|----------|
| `http-proxy-middleware` | `@angular-devkit/build-angular` | HIGH | [GHSA-gcq2-9pq2-cxqm](https://github.com/advisories/GHSA-gcq2-9pq2-cxqm), [GHSA-64mm-vxmg-q3vj](https://github.com/advisories/GHSA-64mm-vxmg-q3vj) |

## Fix required

`npm audit fix` cannot auto-fix these — the fix requires a **semver major bump**:

```
@angular-devkit/build-angular  →  19.2.8  (breaking change)
```

**Manual steps:**
1. Upgrade `@angular-devkit/build-angular` to `^19.x` (and align `@angular/*` packages to v19)
2. Run `npm install` then `npm audit` to verify clean

Generated by /security-scan.
