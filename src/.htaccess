RewriteEngine On

# SSL renewal: ACME challenges must be served as-is, before the SPA fallback
RewriteRule ^\.well-known/acme-challenge/ - [L]

RewriteBase /
RewriteRule ^index\.html$ - [L]
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]
